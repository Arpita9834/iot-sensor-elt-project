-- View all raw data
SELECT * FROM raw_sensor_data;

-- View daily average temperatures
SELECT * FROM daily_avg_temp;

-- View hourly average temperatures
SELECT * FROM hourly_avg_temp;

-- Identify faulty sensor data (optional)
SELECT * FROM raw_sensor_data
WHERE temperature < -50 OR temperature > 150;
